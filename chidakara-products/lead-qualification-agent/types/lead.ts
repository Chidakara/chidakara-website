export interface LeadAnswer {
  questionId: number;
  answer: string;
}
export interface Lead {
  companyType: string;
  employees: string;
  revenue: string;
  challenge: string;
  budget: string;
  timeline: string;
}